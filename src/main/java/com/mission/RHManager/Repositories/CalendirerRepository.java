package com.mission.RHManager.Repositories;

import com.mission.RHManager.Entites.Calendrier;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CalendirerRepository  extends JpaRepository<Calendrier, Long> {
}
